policies:
  - name: KerasPolicy
    epochs: 111 
    max_history: 5
  - name: MemoizationPolicy
    max_history: 4
  - name: FormPolicy
  # other policies
  # - name: RulePolicy # (coming in 2.0)
  - name: "FallbackPolicy"
    nlu_threshold: 0.15
    core_threshold: 0.15
    fallback_action_name: 'action_chat'
